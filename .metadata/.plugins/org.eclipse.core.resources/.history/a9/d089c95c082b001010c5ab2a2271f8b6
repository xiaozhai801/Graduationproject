<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>所有文章</title>
    <link href="../../layui/css/layui.css" rel="stylesheet">
    <script src="../../layui/layui.js"></script>
    <script src="../../js/jquery-3.6.0.min.js"></script>
    <style>
        html,
        body {
            overflow: hidden;
            font-family: 'Inter', sans-serif;
            background-color: #f9fafb;
        }

/*         /* 搜索表单样式 */
        .layui-form {
            background-color: #fff;
            padding: 16px;
            border-radius: 8px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            margin-bottom: 16px;
        } */

        .layui-input {
            border-radius: 4px;
            border: 1px solid #d1d5db;
            transition: border-color 0.3s ease;
        }

        .layui-input:hover {
            border-color: #9ca3af;
        }

        .layui-btn {
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .layui-btn:hover {
            background-color: #e5e7eb;
        }

        /* 表格样式 */
        #articleTable {
            border-collapse: collapse;
            width: 100%;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        #articleTable th,
        #articleTable td {
            border: none;
            border-bottom: 1px solid #e5e7eb;
            padding: 12px 16px;
            text-align: left;
        }

        #articleTable th {
            background-color: #f3f4f6;
            font-weight: 600;
            color: #374151;
        }

        #articleTable tbody tr:last-child td {
            border-bottom: none;
        }

        /* 鼠标悬停效果 */
        #articleTable tbody tr:hover {
            background-color: #f9fafb;
        }

        /* 链接样式 */
        .layui-table-link {
            color: #2563eb;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .layui-table-link:hover {
            color: #1d4ed8;
            text-decoration: underline;
        }

        /* 下拉菜单样式 */
        .layui-dropdown {
            border-radius: 4px;
        }
    </style>
</head>

<body>
    <!-- 搜索表单 -->
    <form class="layui-form layui-row layui-col-space16">
        <div class="layui-form-item">
            <!-- 文章ID输入 -->
            <div class="layui-input-inline">
                <input type="text" name="titleId" placeholder="文章ID" class="layui-input" style="width: 150px;">
            </div>
            <!-- 标题输入 -->
            <div class="layui-input-inline">
                <input type="text" name="topic" placeholder="标题" class="layui-input" style="width: 150px;">
            </div>
            <!-- 作者输入 -->
            <div class="layui-input-inline">
                <input type="text" name="name" placeholder="作者" class="layui-input" style="width: 150px;">
            </div>
            <!-- 机型下拉菜单 -->
            <div class="layui-input-inline">
                <div class="layui-btn-container">
                    <button class="layui-btn" type="button" id="modelDropdown" style="width: 120px;">
                        <span>机型</span>
                        <i class="layui-icon layui-icon-down layui-font-12"></i>
                    </button>
                </div>
                <!-- 隐藏字段保存机型值 -->
                <input type="hidden" name="model" id="modelInput">
            </div>
            <!-- 搜索按钮 -->
            <div class="layui-input-inline">
                <button class="layui-btn" lay-submit lay-filter="searchBtn">
                    <i class="layui-icon layui-icon-search"></i>
                </button>
            </div>
        </div>
    </form>

    <!-- 表格容器 -->
    <table id="articleTable" lay-filter="articleTable"></table>

    <script>
        layui.use(['table', 'form', 'dropdown'], function () {
            var table = layui.table;
            var form = layui.form;
            var dropdown = layui.dropdown;
            var $ = layui.$;

            // 初始化机型下拉菜单
            dropdown.render({
                elem: '#modelDropdown',
                data: [
                    { title: '手机', id: 100 },
                    { title: '笔记本电脑', id: 101 },
                    { title: '游戏主机', id: 102 }
                ],
                click: function (obj) {
                    $('#modelDropdown span').text(obj.title); // 更新按钮显示
                    $('#modelInput').val(obj.title); // 更新隐藏字段
                }
            });

            // 初始化表格
            table.render({
                elem: '#articleTable',
                url: '../../SelectAllArticleServlet',
                page: true,
                cols: [
                    [
                        {
                            field: 'topic',
                            title: '标题',
                            width: 740,
                            templet: function (d) {
                                return '<a class="layui-table-link" href="CurrentArticle.html?titleId=' + d.titleId + '">' + d.topic + '</a>';
                            }
                        },
                        { field: 'name', title: '作者', width: 120, sort: true },
                        { field: 'uploadTime', title: '发布日期', width: 200, sort: true },
                        { field: 'model', title: '机型', width: 100 },
                        { field: 'views', title: '浏览量', width: 100 },
                        { field: 'likes', title: '点赞量', width: 100 }
                    ]
                ],
                height: 'full-100',
                parseData: function (res) {
                    // 仅在初始化时过滤 release=1 的数据
                    if (res.code === 0 && this.url.indexOf('SelectAllArticleServlet') !== -1) {
                        res.data = res.data.filter(function (item) {
                            return item.release == 1;
                        });
                    }
                    return res;
                }
            });

            // 模拟搜索按钮点击后的操作，进行一次重载（传递空参数，可根据需求修改）
            var initialField = {};
            initialField.model = $('#modelInput').val();
            table.reload('articleTable', {
                url: '../../SearchArticleServlet',
                where: initialField,
                page: { curr: 1 }
            });

            // 搜索提交事件
            form.on('submit(searchBtn)', function (data) {
                var field = data.field;
                // 显式传递机型参数
                field.model = $('#modelInput').val();

                // 重载表格
                table.reload('articleTable', {
                    url: '../../SearchArticleServlet',
                    where: field,
                    page: { curr: 1 }
                });
                return false;
            });
        });
    </script>
</body>

</html>    